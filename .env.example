# Supabase Configuration
# Get these values from your Supabase project dashboard
# Project Settings > API
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Mapbox Configuration
# Get your token from https://account.mapbox.com/
EXPO_PUBLIC_MAPBOX_TOKEN=your-mapbox-token

# Google OAuth (configured in Supabase)
# 1. Go to Supabase Dashboard > Authentication > Providers
# 2. Enable Google provider
# 3. Add your Google OAuth client ID and secret
# 4. Add redirect URL: kapkurtar://auth/callback

# Note: Make sure to configure the same Supabase project as the Sepet web app
# to share the same database and authentication
